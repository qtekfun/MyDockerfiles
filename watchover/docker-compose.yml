---
version: '3'

services:
  watchtower:
    container_name: watchover
    image: containrrr/watchtower:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 21600 --cleanup --label-enable
